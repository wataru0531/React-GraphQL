"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("@nestjs/common");
const injector_1 = require("@nestjs/core/injector/injector");
const serialized_graph_1 = require("@nestjs/core/inspector/serialized-graph");
const testing_1 = require("@nestjs/testing");
const app_module_1 = require("../code-first/app.module");
describe('Serialized graph', () => {
    let testingModule;
    beforeAll(async () => {
        vi.spyOn(injector_1.Injector.prototype, 'getNowTimestamp').mockImplementation(() => 0);
        testingModule = await testing_1.Test.createTestingModule({
            imports: [app_module_1.ApplicationModule],
        }).compile({ snapshot: true, preview: true });
    });
    it('should generate a post-initialization graph and match snapshot', async () => {
        const app = testingModule.createNestApplication({ preview: true });
        app.useGlobalPipes(new common_1.ValidationPipe());
        await app.init();
        const graph = testingModule.get(serialized_graph_1.SerializedGraph);
        expect(graph.toString()).toMatchSnapshot();
    });
});
