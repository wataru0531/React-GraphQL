"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const graphql_1 = require("@nestjs/graphql");
const testing_1 = require("@nestjs/testing");
const graphql_2 = require("graphql");
const federation_posts_module_1 = require("../graphql-federation/posts-service/federation-posts.module");
describe('GraphQL federation GraphQLSchemaHost using', () => {
    let app;
    beforeEach(async () => {
        const module = await testing_1.Test.createTestingModule({
            imports: [federation_posts_module_1.AppModule],
        }).compile();
        app = module.createNestApplication();
        await app.init();
    });
    it(`GraphQLSchemaHost should contain schema`, () => {
        const schemaHost = app.get(graphql_1.GraphQLSchemaHost);
        expect(schemaHost.schema).toBeInstanceOf(graphql_2.GraphQLSchema);
    });
    afterEach(async () => {
        await app.close();
    });
});
